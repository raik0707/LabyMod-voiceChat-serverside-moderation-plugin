package de.raik.voicechatmod.core.implementation.join;

/**
 * Enum representing the two different join
 * types a join listener can have
 *
 * @author Raik
 * @version 1.0
 */
public enum JoinType {
    JOIN, //The join type for login process for handling and allowence
    LOGIN //The login call if the player joined a server
}
